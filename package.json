{
  "name": "egg-notifier",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm.cmd --prefix frontend install && npm.cmd --prefix frontend run build && npm.cmd --prefix backend install",
    "start": "node index.js",
    "dev": "concurrently \"npm.cmd run dev --prefix backend\" \"npm.cmd run dev --prefix frontend\""
  },
  "dependencies": {
    "axios": "^1.6.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-session": "^1.17.3",
    "passport": "^0.6.0",
    "passport-discord": "^0.1.4",
    "sqlite": "^5.1.1",
    "sqlite3": "^5.1.6"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
