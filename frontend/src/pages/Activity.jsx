import { ArrowUpRight, Bell, CheckCircle2, Egg, Moon } from 'lucide-react';
import Chip from '../components/Chip';

const icons = { hatch: Egg, sync: Bell, alert: Moon };
export default function Activity({ activity }) {
  const delivered = activity.filter((item) => item.status === 'Delivered').length;
  return <div className="page"><header className="page-header"><div><span className="eyebrow">OVERVIEW / ACTIVITY</span><h1>Good morning, operator.</h1><p className="muted">Here is what your notifier has been up to.</p></div><div className="live-pill"><span className="status-dot" /> Live</div></header><section className="metric-grid"><div className="metric"><span className="metric-label">Signals this week</span><strong>24</strong><span className="metric-trend">+18% from last week</span></div><div className="metric"><span className="metric-label">Delivery rate</span><strong>99.8%</strong><span className="metric-trend">All channels healthy</span></div><div className="metric accent"><span className="metric-label">Next quiet hours</span><strong>22:00</strong><span className="metric-trend">Resumes at 07:00</span></div></section><section className="activity-section"><div className="section-heading"><div><span className="eyebrow">RECENT SIGNALS</span><h2>Activity log</h2></div><button className="text-button">View all <ArrowUpRight size={15} /></button></div><div className="activity-list">{activity.map((item) => { const Icon = icons[item.type] || Bell; return <article className="activity-row" key={item.id}><div className={`activity-icon ${item.type}`}><Icon size={18} /></div><div className="activity-content"><strong>{item.title}</strong><p>{item.detail}</p></div><div className="activity-meta"><span>{item.time}</span><Chip tone={item.status === 'Muted' ? 'muted' : 'success'}>{item.status === 'Muted' ? <Moon size={12} /> : <CheckCircle2 size={12} />} {item.status}</Chip></div></article>; })}</div></section></div>;
}
