import { ArrowUpRight, Bell, CheckCircle2, Egg, Moon } from 'lucide-react';
import Chip from '../components/Chip';

const icons = { hatch: Egg, sync: Bell, alert: Moon };
export default function Activity({ activity, eggs = [] }) {
  const delivered = activity.filter((item) => item.status === 'Delivered').length;
  return <div className="page"><header className="page-header"><div><span className="eyebrow">OVERVIEW / ACTIVITY</span><h1>Good morning, operator.</h1><p className="muted">Here is what your notifier has been up to.</p></div><div className="live-pill"><span className="status-dot" /> Live</div></header><section className="metric-grid"><div className="metric"><span className="metric-label">Eggs tracked</span><strong>{eggs.length}</strong><span className="metric-trend">Latest records from Railway</span></div><div className="metric"><span className="metric-label">Notifications</span><strong>{delivered}</strong><span className="metric-trend">Delivered to your account</span></div><div className="metric accent"><span className="metric-label">Rare eggs</span><strong>{eggs.filter((egg) => ['Secret', 'Mythic', 'Titan', 'Divine', 'Legendary', 'Eternal'].includes(egg.rarity)).length}</strong><span className="metric-trend">Tracked in the latest feed</span></div></section><section className="activity-section"><div className="section-heading"><div><span className="eyebrow">LATEST EGGS</span><h2>Egg records</h2></div><button className="text-button">View all <ArrowUpRight size={15} /></button></div><div className="activity-list">{eggs.map((egg) => <article className="activity-row" key={egg.uid}><div className="activity-icon hatch"><Egg size={18} /></div><div className="activity-content"><strong>{egg.pet || egg.egg}</strong><p>{egg.egg} · {egg.rarity || 'Unknown'} · {egg.mutation || 'None'}</p></div><div className="activity-meta"><span>{egg.biome || egg.area || 'Unknown'}</span><Chip tone="success"><CheckCircle2 size={12} /> Active</Chip></div></article>)}{eggs.length === 0 && <p className="muted">No egg records yet. Send a spawn from Roblox to populate this feed.</p>}</div></section></div>;
}
